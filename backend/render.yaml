services:
  - type: web
    name: retro-snake-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GMAIL_ADDRESS
        value: rajdipnaskar973@gmail.com
      - key: GMAIL_APP_PASSWORD
        value: your16letterpassword